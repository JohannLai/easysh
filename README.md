# easysh
Use of  `??` command to replace complex and obscure shell commands into natural language.

# Install

```sh
# brew
brew install esaysh
```

```sh
# npm 
npm i easysh -g
```

```sh
# yum
yum install easysh
```

# usage
```sh
❯ ?? list all file
❯ ls -a
```
